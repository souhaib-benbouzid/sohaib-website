{
  "name": "@soh/root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && next dev",
    "build": "cd apps/web && next build",
    "start": "cd apps/web && next start",
    "lint": "cd apps/web && next lint",
    "test": "cd apps/web jest --watch",
    "storybook": "cd packages/storybook && yarn storybook",
    "build-storybook": "cd packages/storybook && yarn build-storybook"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
